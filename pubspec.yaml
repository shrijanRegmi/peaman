name: peaman
description: A service for building social media applications with ease!
version: 1.0.0
homepage: https://peaman.web.app
repository: https://github.com/shrijanRegmi/peaman
issue_tracker: https://github.com/shrijanRegmi/peaman/issues

environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  cloud_firestore: ^4.2.0
  cloud_functions: ^4.0.6
  device_info: ^2.0.3
  either_dart: ^0.3.0
  firebase_auth: ^4.2.0
  firebase_core: ^2.4.0
  firebase_messaging: ^14.1.4
  firebase_storage: ^11.0.7
  flutter:
    sdk: flutter
  flutter_facebook_auth: ^5.0.6
  freezed_annotation: ^2.2.0
  google_sign_in: ^6.0.2
  json_annotation: ^4.8.0
  tuple: ^2.0.1

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  freezed: ^2.3.2
  json_serializable: ^6.6.1

flutter: null
